apiVersion: apps/v1
kind: Deployment

metadata:
  name: backend

spec:
  replicas: 2

  selector:
    matchLabels:
      app: backend

  template:
    metadata:
      labels:
        app: backend

    spec:
      containers:
      - name: backend
        image: aryasingh1234/amazone_clone:backend-v2

        ports:
        - containerPort: 8000

        env:
        - name: MONGODB_HOST
          value: mongodb-service